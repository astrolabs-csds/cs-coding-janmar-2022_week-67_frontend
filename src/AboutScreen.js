import NavBar from "./NavBar"

function AboutScreen(props) {

    return (
        <div>

            <NavBar {...props}/>
            <h1>About Screen</h1>

        </div>
    )
};

export default AboutScreen;